---
'penpot-exporter': minor
---

Add a "Choose pages" export scope so a specific set of pages can be selected for export, alongside the existing "All pages" and "Current page" options. The export progress now also shows the name of the page currently being scanned.
